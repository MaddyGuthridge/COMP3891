Page table built using a hash table to map from virtual addresses to physical addresses.

- Given virtual address
- Hash it to get the key
- Look up that key
- Get the page
- Calculate the physical address